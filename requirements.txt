requests>=2.31.0
beautifulsoup4>=4.12.0
playwright>=1.44.0
python-dotenv>=1.0.0

# Optional — only needed if pushing events to Google Calendar directly.
# Remove these two lines if you only want the .ics file approach.
google-api-python-client>=2.100.0
google-auth>=2.20.0
